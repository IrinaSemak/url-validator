# Валидатор URL-адресов (Вариант 15)

Проверяет список URL из файла и выводит отчёт о недоступных ссылках.

**Сборка:** `./gradlew build`  
**Запуск:** `java -jar build/libs/url-validator-1.0.0.jar`  
**Конфиг:** app.properties